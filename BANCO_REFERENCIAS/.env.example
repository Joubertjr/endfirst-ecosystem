# Google Gemini API (OBRIGATÓRIO)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# Google File Search Store ID
# Será criado automaticamente se não especificado
FILE_STORE_ID=

# PostgreSQL (configurado no docker-compose.yml)
# Não precisa configurar - será criado automaticamente pelo docker-compose
POSTGRES_USER=banco_ref
POSTGRES_PASSWORD=banco_ref_password
POSTGRES_DB=banco_referencias

# Environment
ENVIRONMENT=development
DEBUG=true
